# Optional: GitHub OAuth Device Flow
# Create a GitHub OAuth App at https://github.com/settings/developers
# Set "Device flow" to enabled, set redirect to http://localhost
# Then paste the Client ID below.
# Without this, the Sign in with GitHub button won't appear — use a PAT instead.
VITE_GITHUB_CLIENT_ID=
